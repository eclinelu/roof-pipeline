## Current state

- **Project goal (2026-07-21):** engineering PORTFOLIO project. Commercialization, customer acquisition and pricing are dropped after a market analysis found no viable commercial path for a part-time solo operator. Technical scope and the validation standard are unchanged.
- **SITE ROLES (2026-07-27), binds everything below:** big_house is the **DEVELOPMENT** site, unlimited visual inspection and iteration, and **no accuracy claim ever comes from it**. bungalow and cove_house are **HELD-OUT VALIDATION**, rules frozen before either runs, each scored ONCE. **roger is lost**, so there are exactly TWO held-out sites and ONE attempt at each. Standing limit: visual review establishes THAT something is wrong and WHAT it is physically, and can NEVER set a parameter value.
- **Phase (2026-07-30):** defect-fixing loop. **THE GRID ADOPTION IS EXECUTED, SCORED AND ADOPTED.** M1a stays CLOSED as MEASURED AND DEPRIORITIZED. The pitch-bias hypothesis stays **ACCEPTED, UNTESTED**, a KNOWN LIMIT under 3e.
- **CANONICAL STATE (changed 2026-07-30):** `reports/big_house/canonical-2026-07-30-grid-adopted.json` + `.npz`, 29 facets (8 main + 21 recovered), 17 blobs. **Published facet coverage is now 94.25 pct**, up from 88.40. Both predecessors are untouched on disk and remain recomputable: `canonical-2026-07-26-r2` (88.40) and `canonical-2026-07-28-grid` (94.25).
- **PUSH STATE: CLEAN.** All three pre-registrations are on origin and verified with `git branch -r --contains`: `2b28704` (M1a), `7a64185` (pitch bias), `e352045` (grid adoption, pushed BEFORE the run).
- **NEXT ACTION, Emmett's:** **PASS 2 REVIEW IS OWED AND NOT RUN.** It is now unblocked: 29 per-facet PNGs plus an overview were rendered from the adopted state into `reports/big_house/review/2026-07-30-grid-adopted/` (open `review.html`). Graded BLIND with the diff toggle OFF, free text only, Emmett's eyes not a computation. Then the next mechanisms by stage order are **M2** (3 undiscovered dormer surfaces) and **M3** (5 facet pairs on one real plane), both stage 2, both ahead of boundary work.

- **THE 2026-07-30 ADOPTION RUN, five of five predictions PASS.** `decisions/2026-07-30-grid-adoption-executed-and-scored.md`, scored in `reports/big_house/grid-adoption-score-2026-07-30.json` against the pre-registration `e352045`.
  - P1 the 8 main facets are bit-identical to r2, combined hash `e1df986e...eea64824` equal to the pre-registered prior. P2 the deliverable stays 3,559.3 ft^2 over 8 facets. P3 coverage reproduces 94.25 pct (272.618 / 289.237 cu^2). P4 phase spread 0.1800. P5 recovery stays 21 and stays out of the total. Five independent assertions also passed, including an anti-null that the scorer is not reading the reference artifact.
  - **P4 passed EXACTLY at its threshold**, 0.1800 against a `<= 0.18` line. Recorded as-is, because a prediction landing on its boundary is weaker than one clearing it.
  - **Unpredicted and stronger than anything asked for: ALL 29 facets, not just the 8 main ones, are BIT-IDENTICAL to `canonical-2026-07-28-grid`**, with every coverage and footprint value equal. Two runs, two days apart, separate processes. That is whole-pipeline determinism including recovery.
  - The phase sweep reproduced the decomposition exactly: pitch fix **+5.93** points, origin fix **+1.11**, combined **+5.87** (88.40 -> 94.27). Its anti-null passed: the SUPERSEDED configuration still reproduces 88.40 pct and 299.654 cu^2 exactly.
- **NEW ASSERTIONS IN THE CODE (2026-07-30), no parameter changed.** (1) A shared-lattice assertion in `coverage_masks`: `exact_pitch` on, origin equal to `floor(min/cell)*cell` from one shared global min, and **`Hexp <= Hall` in every cell**, which is the only one of the three built from the DATA rather than the arguments. (2) An opt-in `--assert-main-hashes STAMP` in `canonical_state.py` that runs BEFORE anything is written, so a run that disturbed the frozen facets leaves no artifact behind. Pitch and area are deliberately excluded from the hash, being functions of the indices and plane.
  - **The guard is opt-out via `allow_superseded=True` on exactly four probes** that deliberately recompute superseded numbers. An unconditional guard broke all four and would have made superseded numbers unrecomputable, which is the property "supersede, never overwrite" depends on.
  - **Measured while testing it: under the superseded configuration `Hexp > Hall` in 707,650 cells on big_house.** The 2026-07-28 grid defect counted directly for the first time rather than inferred from coverage moving. Nothing adopted from it.
- **OPEN, DISCOVERED 2026-07-30: THE PUBLISHED PER-FACET AREAS DO NOT RECOMPUTE.** On facets that are **provably identical** (all 8 point counts match `preregistered-2026-07-18.json` exactly: 1538098, 1154335, 693991, 850268, 744303, 669484, 403109, 297583), recomputing slope area today gives **3,602.0 ft^2 with per-facet spacing (+42.7, +1.2 pct)** or **3,590.5 ft^2 with global spacing (+31.2)** against the published **3,559.3**. Neither convention reproduces it, and `alpha_mult` is 4.0 in both configs. Three explanations tested and killed: different membership, different multiplier, per-facet-vs-global spacing. **Nothing was fixed and no area was recomputed into any artifact.** 3,559.3 ft^2 stands as a frozen 2026-07-18 figure. `decisions/2026-07-30-published-areas-do-not-recompute.md`. Reassuring half: main discovery has been STABLE since 2026-07-18.
- **ULTRA COMPLETED ONCE, 2026-08-03, RECONSTRUCTION ONLY.** big_house rebuilt clean at `--pc-quality ultra` into `C:\odm\datasets\big_house_ultra`. ODM 3.6.0, `success: true`, **172 min**, cloud `odm_georeferencing/odm_georeferenced_model.laz` 302.8 MB, **90,151,819 points against 21,325,293 for the medium canonical cloud, 4.23x**, same scene (p1-p99 extents agree to 0.6 pct on every axis; the 31.9 pct header-extent gap is outlier reach, and p1-p99 Z is 8.25 on BOTH). `decisions/2026-08-03-ultra-is-conditional-on-the-memory-configuration.md`.
  - **THE ULTRA DEFAULT IS CONDITIONAL ON THE MEMORY CONFIGURATION, and `swap` is what carries it, not RAM.** At the fusion peak the run held **22.390 GiB of the 23.470 GiB cap (95.4 pct) AND ~12.6 GiB of swap simultaneously**, a working set near **35 GiB**. The config for the two OOM-killed attempts was 15.35 GiB RAM + 4 GiB swap = 19.4 GiB. `C:\Users\eclin\.wslconfig` (`memory=24GB`, `swap=32GB`) is **part of the reconstruction environment**, in the same sense as the ODM version. It lives outside the repo, which the entry states as a real weakness in the record.
  - **One success is not reliability.** RAM peaked at 95.4 pct of cap. Narrow margin, not headroom. **The analysis pass has NOT been run on this cloud.**
  - **"One change only" was made checkable:** the two runs' `log.json` option dicts diffed programmatically, 91 options each side, exactly one difference, `pc_quality: medium -> ultra`.
  - **Two things found on the way, reported not fixed.** (1) The canonical medium cloud was built with **`skip_3dmodel = False`**, so meshing and texturing ran, contradicting `CLAUDE.md`'s instruction that `--skip-3dmodel` is always required; this run matched the baseline rather than add a second change. (2) The canonical medium cloud came from a **RESUMED** run (`rerun_from` = `odm_filterpoints` onward); this ultra run was clean.
- **RECORDED 2026-07-30, gating nothing:** **bungalow will be reprocessed at ultra**, and the **plan-view render gate before ultra is SKIPPED by decision**. The per-facet PNGs from this run are the BASELINE for the post-ultra side-by-side. **Bungalow is still NOT reconstructed**; it was gated on ultra completing once, which has now happened.
- **LINE ENDINGS PINNED (2026-07-30).** `.gitattributes` covered only the decision log; measured with `git ls-files --eol`, **55 files under `reports/` were LF in the blob and CRLF in the working tree**, both canonical states among them. Now `reports/**/*.json` and `**/*.md` are `text eol=lf`, `**/*.npz` and `**/*.png` are `-text`. `-text` on the JSON was rejected: it would have rewritten all 55 frozen blobs from LF to CRLF, changing every frozen artifact's hash for no change in content.

- **PITCH BIAS: SAWTOOTH ACCEPTED, UNTESTED. KNOWN LIMIT under 3e (2026-07-29).** The 1.83 deg systematic offset (pipeline reads steeper than the hand inclinometer on essentially every facet) is accepted as shingle-face-versus-deck. **NOT confirmed.** Stripes FAILED to appear at a resolution that resolves a 5 in course, amplitude came in 5 to 17x below prediction, the shape discriminator fit NEITHER band. **A null there is not a refutation:** MVS smooths, so a sawtooth below the resolving power is averaged away, and the average of a shingle sawtooth IS the deck slope, which is what the hypothesis requires. The tests are uninformative, not negative. **NO FURTHER TESTS.** **REPORTING REQUIREMENT: the limit must be stated ATTACHED TO ANY PITCH NUMBER in any report, never in an appendix.** **NO CORRECTION is applied to any pitch on any facet at any site, now or later, without a new decision.** Confirmed untouched by the 2026-07-30 pass.
- **M1a DOES NOT MOVE THE DELIVERABLE (2026-07-28).** Frozen pitch max |error| 2.19 deg, headroom to the 3 deg line 0.81 deg. At the loosest setting max error goes to 2.188 (-0.0020 deg); at the canonical setting to 2.2021 (+0.0121 deg). **No facet's verdict flips at 3 deg or at 2 deg.** The **1.83 deg systematic mean bias dominates it by 151x**. **Untouched: AREA**, and big_house has no measured area to move against.
- **M1a RESULT in one paragraph.** The connectivity filter works, is correct under six independent assertions, and has **no stable operating region on this building**. 16 distinct answers over 20 grid points. Below about 2.5 x spacing it destroys real roof (838,168 points removed at 1.5 x spacing, against 3,134 at the loosest, a factor of 267). The filter is in the codebase OPT IN, defaulting off. **M1a ATTEMPT 2 IS DRAFTED AND DELIBERATELY NOT RUN** (`docs/draft-m1a-attempt-2-preregistration.md`). Two attempts at M1a, not more.
- **M1a AREA EXPOSURE CLOSED.** Total alpha area of removed components **80.57 ft^2 = 2.26 pct** of the reported 3,559 ft^2. The render shows the removed material is long coherent LINES following ridges and hips, so it is an ATTRIBUTION error on shared edges, routing to boundary regularization rather than a third membership filter.
- **RENDERED (R6): the 6 coverage points are INTERIOR SPECKLE, not a rim.** 96.1 pct of the 69,047 gained cells sit >4 cells inside the old explained region, median depth 105 cells. The old grid was punching holes through well-covered roof. A drift-gradient prediction of Claude's FAILED (non-monotone); attribution rests on the counterfactual.
- **Blob 0, CLARIFIED 2026-07-28 (no reversal).** Under the PRODUCTION config blob 0 has **no candidate at all**: its dominant plane, 162,938 points at 3.774 deg, is discarded by the recovery PITCH WINDOW (min_pitch 5.0) before quality is tested. **P5 of the older scorecard asked a question the production pipeline never evaluates.** Blob 0 is confirmed REAL ROOF by inspection; its region is the capture-poor part, **68.18 pct against 83.37 pct for the rest of the roof**.
- **CAPTURE, on the adopted grid:** density-testable **82.72 pct** (289.237 / 349.640 cu^2), one-point cells **227,964**, enclosed holes **111,579**, p10 points-per-occupied-cell **1.0**. Raw-vs-raw, both recomputed on the same config: **bungalow 67.14 pct, big_house 60.10 pct**, so the operator-and-habits reading survives, bungalow better by about 7 points.
- **Footprint residue NOT fixed, and required on any footprint claim:** filled footprint spread **4.40 cu^2**, eroded **6.20 cu^2**, from `min_pts=2` discretisation, not from the grid. Raw footprint stable at 0.121 cu^2.
- **Unassigned footprint, split into its two causes (2026-07-27):** segmentation gap **33.682 cu^2 = 382.8 ft^2**; capture gap **49.902 cu^2 = 567.1 ft^2**, of which filled holes 44.974.

- **TRIAGE RESULT (2026-07-27), seven mechanisms. 42 of 56 defect instances (75 pct) fit NEITHER pre-registered hypothesis:**

      M1  membership by distance to an UNBOUNDED plane      stage 1   11 facets   <- M1a CLOSED 2026-07-28
          M1a thin remote slivers                                     8 (all main)
          M1b bulk absorption of adjacent structure                   3 (8, 9, 10)
      M4  boundary stops inside the real edge               stage 4   17 facets
      M5  boundary ragged where the real edge is straight   stage 4   12 facets
      M3  one real plane fitted as TWO facets               stage 2   10 facets
      M2  dormer surfaces never discovered                  stage 2    3 surfaces
      M6  outline crosses a correctly-found ridge           stage 4    2 facets
      M7  neighbours disagree on a shared boundary          stage 4    1 facet

  M4 is the largest and was not pre-registered. Five defects are recorded UNATTRIBUTED and deliberately not forced into a bucket. Fix order is by pipeline STAGE, not severity.
- **CANDIDATE, logged 2026-07-27, NOT ADOPTED:** boundary regularization, facet boundaries as straight lines with a shared boundary being ONE line. Eliminates M6, M7, M5 and part of M4 by construction. Blocked behind M2 and M3. Recorded as a MODEL CHANGE, not a bug fix. Stated cost: regularization can HIDE CAPTURE FAILURES and would break the detector that found blob 0; mitigation is to keep measured extent and regularized model as separate permanent outputs plus their divergence.

- **Standing rules (permanent):** R1 every run persists per-facet plane coefficients and inlier indices. R2 actionable diagnostics go to a file, never stdout only. R3 (2026-07-27) any depth-from-boundary or erosion measurement FILLS HOLES FIRST. R4 (2026-07-27) every diagnostic carries an assertion built from something known INDEPENDENTLY of its own result; register at `reports/silent-failures/README.md`. **R4 extension (2026-07-28): a probe that perturbs a parameter must assert that at least one perturbation changed at least one output.** R5 (2026-07-28) raster PHASE is a parameter: any filter that rasterises and then reasons about adjacency must sweep the origin phase, be invariant by construction, or fix the origin to something meaningful. R6 (2026-07-28) a finding about the BUILDING must be RENDERED before adoption; instrument findings may stay numeric.
- **REQUIRED FIELD: POWER CHECK in every pre-registration (2026-07-29).** Each prediction states, alongside its direction, what must be true for the test to SEPARATE its hypotheses, computed BEFORE the run. **A rigorous procedure applied to an underpowered test launders noise into a finding.** The 2026-07-30 pre-registration used it to state in advance that P1, P2 and P5 were regression guards rather than discoveries, and that P3 and P4 carried the information.
- **Review loop, from pass 2 onward:** every pass produces a DIFF against the previous artifact, **behind a toggle, default OFF**. The review UI drops multiple choice for FREE TEXT ONLY. Mechanism attribution runs through Claude reading Emmett's prose: propose it and show it for confirmation, never infer a verdict silently.
- **OPEN DEFECT, must be settled before the bungalow freeze:** `scripts/canonical_state.py` calls `discover_facets` without `min_pitch`, so main discovery takes the default 10.0 while the file it writes records 5.0. Verified INERT on big_house. Not changed: it alters the production config and that is Emmett's call.
- **HELD, not cancelled:** Diagnostic E (alpha-shape area characterisation, alpha-radius plateau sweep, large-void flag) and the candidate fringe-attribution rule. Area pre-registration is deferred until E returns. **The 2026-07-30 area finding above should be settled inside that work, not before it.**
- **A statistic of Claude's was INVALID and is corrected in the record:** peak/background over a RED spectrum reported up to 318 where the detrended excess is 9 dB. Retained as `naive_ratio_INVALID` so the correction is auditable.

- **Last thing verified working (2026-08-03):** the ultra reconstruction of big_house, 172 min, `success: true`, verified by two INDEPENDENT completion checks (the `Finished odm_georeferencing stage` log line AND the `.laz` on disk) rather than by the container exiting; the one-change flag diff (91 vs 91 options, one difference); the extent probe showing the density comparison is same-scene; `monitor_odm.py --self-test` 18/18 including the two rows-9-and-10 regressions.
- **MONITORING, fixed 2026-08-03 (`fe8e8c6`), both failures logged as silent-failure rows 9 and 10.** Row 9: liveness (CPU, file counts, timestamps) was read as health and a dead run was called HEALTHY three times over ~2 h. Row 10: the supervisor built to fix that then **exited 0/HEALTHY for a run killed by hand**, because it treated the container vanishing as the run ending. Now completion needs POSITIVE evidence from two independent sources or the verdict is `INTERRUPTED`, **exit 4, never 0**; `HEALTHY` states in every report that it means only that no KNOWN failure signature was found.
- **Previously verified (2026-07-30):** the adoption run itself, 193.5 s, with all four in-run assertions passing and all 29 facets bit-identical to the 2026-07-28 artifact; the five-prediction score 5/5 with 5/5 independent assertions; the phase sweep with all four of its checks passing including the superseded-path anti-null; 58/58 unit tests with the new assertions in place; `verify_decisions_split.py` passing at 66 entries, 29 pre-split byte-for-byte identical and in order.
- **Scored (2026-07-18), unaffected:** validation chain complete, both pre-registrations reported, failure first. Run 1 FAILED its scale cross-check at -3.96 pct; Run 2 PASSES at +1.92 pct. Quotable as evidence about cloud-unit geometry, scale and reproducibility, but NOT as an accuracy claim.
- **Results (run 2 scaling, 40.454 in/cu):** total roof area **3,559 ft^2** with a total-level dormer caveat, 8 main facets only, excluding all 21 recovered facets and blob 0. **See the 2026-07-30 entry above before recomputing it.** Pitch: max |error| 2.19 deg, PASS at 3 deg, not at 2 deg, **and the 1.83 deg accepted-untested bias caveat travels with every pitch number**.
