## Current state

- **Project goal (2026-07-21):** engineering PORTFOLIO project. Commercialization, customer acquisition, and pricing are dropped after a market analysis found no viable commercial path for a part-time solo operator (see the 2026-07-21 entry). Technical scope and the validation standard are unchanged.
- **Phase (2026-07-26):** big_house VALIDATED and SCORED (unchanged, see "Scored" below). Active work is the coverage/dormer track. Task 4 (coverage replaces `expected_facets`), Task 5 (diagnostics), Task 6 (determinism, canonical state, merge, empty blobs) and Task 7 (min_pitch, coverage denominator) have all landed. cove_house reconstructed (ODM, 2026-07-25, 160 GPS-tagged stills, 47.62 MB `.laz`) but not yet analysed. bungalow reconstructed but has no crop box yet.
- **Active blocker:** none.
- **Canonical facet state (2026-07-26):** `reports/big_house/canonical-2026-07-26-r2.json` + `.npz`, 29 facets (8 main + 21 recovered), built under `probability=1.0`, `min_pitch=5`, cell-based blob selection, and a derived point floor. Two guards pass on every run: `points[idx]` rebuilds every facet bit for bit, and no point is owned by two facets. `canonical-2026-07-26` (25 facets) and the 2026-07-23 state are SUPERSEDED, not overwritten; their files remain as evidence.
- **Headline coverage numbers (2026-07-26), two metrics not one:** density-testable fraction (CAPTURE quality) **82.29 pct**, 290.256 of 352.707 cu^2; facet coverage (SEGMENTATION) **88.40 pct**, 256.574 of 290.256 cu^2. Footprint reported three ways: raw (cells at >=2 points) 299.654, holes filled 362.105, after one-cell erosion 352.707 cu^2. The previously reported 93.14 pct (2026-07-23) and 93.51 pct (2026-07-26) are RESTATED, not corrected: their denominator had been shrunk about a third by eroding a hole-riddled mask. Coverage was never pre-registered, so restating it costs nothing methodologically.
- **Standing rules (permanent):** R1 every run persists per-facet plane coefficients and inlier point indices to `reports/<dataset>/`, so any run is exactly replayable from its own output regardless of whether the fit is deterministic. R2 any diagnostic Emmett is expected to act on is written to a file under `reports/<dataset>/`, never stdout only.
- **Open question, not a blocker (2026-07-26):** whether RECOVERY should carry its own `min_pitch` separate from MAIN DISCOVERY. At 5 for both, recovery discards four well-fitted planes carrying 166,015 points and 19.04 cu^2 of gross surface, all near 0.8:12; blob 0 alone is 11.57 cu^2 of unexplained plan area, roughly a third of the 33.68 cu^2 that remains unexplained. A recovery-only sweep is measured in `recovery-pitch-sweep-2026-07-26.json` as a DIAGNOSTIC ONLY. No value is adopted; adoption is pre-registered on the next site before that site is scored.
- **Capture-quality finding (2026-07-26), permanent for big_house:** the plan mask is riddled with sub-threshold holes (111,154 enclosed, 370,079 cells, median 1 cell; 249,745 cells inside the footprint hold exactly 1 point). This is a CAPTURE shortfall, not a segmentation failure, and big_house cannot be re-flown. Measured before running anything on bungalow: bungalow raw cloud is 67.09 pct density-testable against big_house raw at 60.68 pct, so the same failure mode is present, slightly less severe. p10 points-per-occupied-cell is 1.0 on both, meaning the sparsest tenth of the surface cannot be tested at all.
- **Site findings (2026-07-15, roof visit):** (1) the fallback ridge span `length:r6,7` was tape-measured on site BEFORE the freeze, so this is NOT a scale-blind pre-registration; the honest claim is only that the cloud-unit geometry is locked before any real-world conversion. (2) the predicted rank-1 primary `lines:j0,3-j0,5` was found NON-EXISTENT on the roof (dormer points corrupted facet 0's junctions), so it is recorded as predicted-but-refuted and deliberately NOT frozen in the candidate slot; the candidate slot holds the taped ridge `length:r6,7`, with `lines:r1,3-e1` as the declared fallback. (3) about 8 dormers were not segmented and contaminate specific per-facet numbers; attribution could NOT be cleaned up (101 entangled clusters; facets 4 and 5 alone are near-clean, facet 2 heaviest).
- **Last thing verified working (2026-07-26):** 58 tests green (11 new, covering single ownership including its failure path, hole filling, the coverage split, and the low-slope boundary). `verify_decisions_split.py` passes: all 29 pre-split decision entries byte-for-byte identical and in order, later entries appended above them.
- **Scored (2026-07-18), unaffected by any of the above:** big_house validation chain complete; both pre-registrations reported, failure first. Run 1 (`6c2e614`, r6,7 = 10.808 cu) FAILED its independent scale cross-check at -3.96% (budget +/-2.47%); diagnosis: the extent jumped a 0.469 cu void to a 97-point assignment-artifact island. Run 2 (`4fe6859`, `preregistered-2026-07-18.json`, contiguity rule, r6,7 = 10.184 cu, all else bit-identical) PASSES at +1.92%. Ground truth committed (`4dd4f92`).
- **Results (run 2 scaling, 40.454 in/cu):** total roof area 3,559 ft^2 WITH a total-level dormer caveat (6 of 8 facets suspect; bias unquantified). Run 1 baseline embedded: 3,160.6 ft^2, +12.6% apart. Pitch: max |error| 2.19 deg, PASS at 3 deg, not at 2 deg; offset decomposes as +1.83 deg uniform + 0.37 deg azimuth term, rms residual 0.24 deg. GPS scale error +2.75% under run 2.
- **Next action:** Emmett's call on the recovery `min_pitch` question above, then the queued work: PDF report refinement (paused 2026-07-20; two data-support gaps left as honest degradations, not defects), then roger capture design (ground-only truth, oblique wall passes, solar-array watch items). Dormer detection deferred until after the three new houses (2026-07-18). bungalow does not run until the recovery-pitch question is settled.
- **Note on the 7a numbers:** totals 313.19 cloud units^2, pitch floor 0.20 deg, pitch classes ~5:12 and ~8:12; leveling values in roofkit.json (1.083 deg, uphill az 75.1); `expected_facets` pinned at 8 as the vanish guard. Per-facet numbers are dormer-suspect except facets 4 and 5.
