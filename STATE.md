## Current state

- **Project goal (2026-07-21):** engineering PORTFOLIO project. Commercialization, customer acquisition and pricing are dropped after a market analysis found no viable commercial path for a part-time solo operator. Technical scope and the validation standard are unchanged.
- **SITE ROLES (2026-07-27), binds everything below:** big_house is the **DEVELOPMENT** site, unlimited visual inspection and iteration, and **no accuracy claim ever comes from it**. bungalow and cove_house are **HELD-OUT VALIDATION**, rules frozen before either runs, each scored ONCE. **roger is lost**, so there are exactly TWO held-out sites and ONE attempt at each. Standing limit: visual review establishes THAT something is wrong and WHAT it is physically, and can NEVER set a parameter value.
- **Phase (2026-07-27):** defect-fixing loop, pass 1. Visual review DONE and triaged. The first fix (M1a) is PRE-REGISTERED AND COMMITTED but NOT WRITTEN AND NOT RUN.
- **NEXT ACTION, and the session ended here:** build the M1a connectivity filter and run its plateau sweep, per `decisions/2026-07-27-m1a-connectivity-preregistration.md`. **Two things are waiting on Emmett:**
  1. The sweep as pre-registered is ~20 full pipeline runs at ~172 s each, about an hour. The alternative is a coarse 3x3 pilot then a fine sweep in the plateau region, but that is a two-stage design and would have to go into the entry BEFORE running.
  2. Claude corrected the premise under P5/P6/P8 (removing a point from membership does NOT unassign it, because `coverage_masks` re-derives assignment from the PLANE, not the membership list). The corrected entry is committed. If Emmett disagrees, the pre-registration needs amending before the run.
- **Active blocker:** none technical.
- **Canonical facet state, UNCHANGED:** `reports/big_house/canonical-2026-07-26-r2.json` + `.npz`, 29 facets (8 main + 21 recovered). Nothing in the 2026-07-27 work regenerated it. Published facet coverage **88.40 pct** stands as the frozen historical number and is not restated; on big_house it is now a DEVELOPMENT READOUT, not a score.
- **TRIAGE RESULT (2026-07-27), seven mechanisms. 42 of 56 defect instances (75 pct) fit NEITHER pre-registered hypothesis:**

      M1  membership by distance to an UNBOUNDED plane      stage 1   11 facets
          M1a thin remote slivers                                     8 (all main)
          M1b bulk absorption of adjacent structure                   3 (8, 9, 10)
      M4  boundary stops inside the real edge               stage 4   17 facets
      M5  boundary ragged where the real edge is straight   stage 4   12 facets
      M3  one real plane fitted as TWO facets               stage 2   10 facets
      M2  dormer surfaces never discovered                  stage 2    3 surfaces
      M6  outline crosses a correctly-found ridge           stage 4    2 facets
      M7  neighbours disagree on a shared boundary          stage 4    1 facet

  M4 is the largest and was not pre-registered. Five defects are recorded UNATTRIBUTED and deliberately not forced into a bucket. Fix order is by pipeline STAGE, not severity; intersection lines get no work item until the facets settle.
- **M1a is measured, not just observed** (`reports/big_house/fragments-2026-07-27.json`): all 8 main facets carry disconnected fragments, extent inflation 1.21-2.40, strays up to 53.4 ft out, facet 0 resolving into 1,076 components. Facets 11-28 measure 1.00-1.09 and are the control.
- **Blob 0: CONFIRMED REAL ROOF by physical inspection (2026-07-27).** Three mechanisms proposed, three dead (two-plane REFUTED, boundary contamination WITHDRAWN, erosion REFUTED). **It is real roof that fails the quality bar, and there is currently no defensible reason to think the bar is wrong.** One candidate remains and is deliberately not probed until after the review passes: fit quality is a residual distance with no normalisation for capture density, and the bar is `max()` over 8 well-captured facets. **Caveat:** if M1a is real then every plane fit is contaminated and every quality score is suspect INCLUDING blob 0's, which would make the blob 0 question not currently well-posed.
- **Standing rules (permanent):** R1 every run persists per-facet plane coefficients and inlier indices. R2 actionable diagnostics go to a file, never stdout only. R3 (2026-07-27) any depth-from-boundary or erosion measurement FILLS HOLES FIRST. R4 (2026-07-27) every diagnostic carries an assertion built from something known INDEPENDENTLY of its own result; register at `reports/silent-failures/README.md`, six instances, two of them inside instruments built to catch the others.
- **Review loop, from pass 2 onward:** every pass produces a DIFF against the previous artifact (per facet: points, pitch, plan area, gross area, quality, component count, plus an outline overlay and a list of what did NOT change), **behind a toggle, default OFF**. The review UI drops multiple choice for FREE TEXT ONLY, because pass 1 showed the codes recorded the opposite of the notes and the note was right every time they disagreed. Mechanism attribution now runs through Claude reading Emmett's prose: propose it and show it for confirmation, never infer a verdict silently, never fill a blank because the note seems clear.
- **OPEN DEFECT, must be settled before the bungalow freeze:** `scripts/canonical_state.py:184` calls `discover_facets` without `min_pitch`, so main discovery takes the default 10.0 while the file it writes records 5.0. Verified INERT on big_house (both reproduce all 8 main facets exactly). Not changed: it alters the production config and that is Emmett's call.
- **HELD, not cancelled:** Diagnostic E (alpha-shape area characterisation, alpha-radius plateau sweep, large-void flag) and the candidate fringe-attribution rule. Area pre-registration is deferred until E returns.
- **Area status:** the tape gave a LENGTH for scale; **no measured area exists for big_house**, so area has never been validated against ground truth and nothing is contaminated by the inspection. The scored total 3,559.3 ft^2 (2026-07-18) is 8 main facets only and excludes all 21 recovered facets and blob 0. No area total has ever been computed from the 29-facet state.
- **Unassigned footprint, split into its two causes (2026-07-27):** segmentation gap (testable roof no facet claims) **33.682 cu^2 = 382.8 ft^2**, exactly coverage's unexplained figure; capture gap (untestable, or outside the eroded interior) **49.902 cu^2 = 567.1 ft^2**, of which filled holes 44.974. They call for opposite responses and are drawn as separate layers.
- **Last thing verified working (2026-07-27):** `verify_decisions_split.py` passes (49 entries, 29 pre-split byte-for-byte identical and in order, 20 appended). `validate_review.py` passes on the completed review. All 2026-07-27 probes carry embedded cross-checks that PASS.
- **Scored (2026-07-18), unaffected:** validation chain complete, both pre-registrations reported, failure first. Run 1 FAILED its scale cross-check at -3.96 pct; Run 2 PASSES at +1.92 pct. Under the new site roles this stays quotable as evidence about cloud-unit geometry, scale and reproducibility, but NOT as an accuracy claim.
- **Results (run 2 scaling, 40.454 in/cu):** total roof area 3,559 ft^2 with a total-level dormer caveat. Pitch: max |error| 2.19 deg, PASS at 3 deg, not at 2 deg. NOTE per P7: the frozen run used the same `discover_facets` path, so whatever pitch delta M1a removal produces also measures how much the frozen pitch numbers were computed on contaminated membership.
