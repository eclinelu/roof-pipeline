## Current state

- **Project goal (2026-07-21):** engineering PORTFOLIO project. Commercialization, customer acquisition and pricing are dropped after a market analysis found no viable commercial path for a part-time solo operator. Technical scope and the validation standard are unchanged.
- **SITE ROLES (2026-07-27), binds everything below:** big_house is the **DEVELOPMENT** site, unlimited visual inspection and iteration, and **no accuracy claim ever comes from it**. bungalow and cove_house are **HELD-OUT VALIDATION**, rules frozen before either runs, each scored ONCE. **roger is lost**, so there are exactly TWO held-out sites and ONE attempt at each. Standing limit: visual review establishes THAT something is wrong and WHAT it is physically, and can NEVER set a parameter value.
- **Phase (2026-07-28):** defect-fixing loop, pass 1. M1a attempt 1 (connectivity) BUILT, RUN and STOPPED, no plateau, nothing adopted. **M1a is NOT closed:** one fix failed, the mechanism is untouched and sits at stage 1 upstream of M2/M3, so under 3b it does not get skipped. Attempt 2 is DRAFTED, not run.
- **PUSH STATE: CLEAN.** `origin/main` = local = `3c7a29b`, 0 ahead / 0 behind. `git branch -r --contains 2b28704` returns `origin/main`, so the M1a pre-registration exists off this machine.
- **NEXT ACTION, and it is a decision for Emmett, not a task:** whether to make the coverage grid self-consistent, which moves published facet coverage from **88.40 pct to about 94.33 pct**. Until that is settled, coverage cannot be used as a cross-pass detector and M1a attempt 2 must either wait or exclude coverage from its scored outcomes. See the two options in `docs/draft-m1a-attempt-2-preregistration.md` section 4.

- **THE BIG FINDING (2026-07-28): THE PUBLISHED 88.40 PCT IS SUBSTANTIALLY A GRID ARTIFACT.** Shifting the production raster's origin by fractions of a cell, changing nothing else, swings facet coverage **87.22 to 92.42 pct, a spread of 5.20 percentage points**. Cause: `plan_grid` (`coverage.py:41`) asks histogram2d for `nx` bins over `[xlo, xhi]` rather than bins of width `cell`, so its bins are 99.978 pct of `cell`, while the sibling `Hexp` histogram (`coverage.py:101`) has bins of exactly `cell`. The two grids `coverage_masks` compares cell-for-cell **drift apart by 0.487 x 0.731 CELLS at the far corner**, and every area is charged as `cell^2`, the pitch of neither. **Counterfactual settles causation:** with `exact_pitch=True` the phase spread collapses **5.20 -> 0.18 points** and coverage reads **94.33 pct**. `decisions/2026-07-28-published-coverage-is-a-grid-artifact.md`. **Nothing adopted; `exact_pitch` defaults to the historical behaviour so nothing moves until someone flips it on purpose.**
  - **Consequence: attempt 1's coverage analysis measured nothing.** Its 0.03-point coverage signal sits two orders of magnitude inside a 5.2-point artifact. The no-plateau conclusion is unaffected, resting on the plateau count, the bar and pitch, none of which are rastered.
  - **NOT affected: PITCH.** It comes from a fitted normal via `tilt_degrees` and never touches a plan raster.
  - **Genuinely phase-sensitive even after the fix:** filled footprint ~1.1 pct, eroded footprint ~1.5 pct. Real discretisation sensitivity from `min_pts=2` with 249,745 one-point cells. Raw footprint stable to 0.06 pct.
  - **Origin audit:** the whole coverage/footprint/residual/recovery chain shares ONE data-derived origin; `area_accounting` (`coverage.py:545`) uses a SECOND; the M1a filter used a THIRD. None was chosen.
- **M1a DOES NOT MOVE THE DELIVERABLE (2026-07-28).** Frozen pitch max |error| 2.19 deg, headroom to the 3 deg line 0.81 deg. Applying the sweep's per-facet deltas: at the loosest setting (3,134 points removed) max error goes to 2.188 (-0.0020 deg); at the canonical setting (151,449 removed) to 2.2021 (+0.0121 deg). **No facet's verdict flips at 3 deg or at 2 deg at either setting.** The effect is 1.5 pct of the margin. The **1.83 deg systematic mean bias dominates it by 151x** and is what actually blocks a PASS at 2 deg. `decisions/2026-07-28-m1a-does-not-move-the-deliverable.md`. **Untouched by this audit: AREA.** Extent inflation is an area-shaped defect and big_house has no measured area, so the one deliverable M1a would most plausibly move has no ground truth to move against.
- **M1a ATTEMPT 2 DRAFTED, NOT RUN, NOT COMMITTED AS A PRE-REGISTRATION:** `docs/draft-m1a-attempt-2-preregistration.md`. Extent-based membership (alpha shape of the plane's subsample discovery inliers, dilated by `d`) instead of connectivity. **No raster, therefore no phase parameter**, and it carries an EQUIVARIANCE assertion (translate the cloud, the partition must be bit-identical) that attempt 1 could not have had. Sweeps `d` and the alpha radius `a`, 5 x 4. Awaiting Emmett's review before any code. **Two attempts at M1a, not more**; if this fails M1a becomes a known limit under 3e.

- **M1a RESULT in one paragraph.** The connectivity filter works, is correct under six independent assertions, and has **no stable operating region on this building**. 16 distinct answers over 20 grid points; the largest group of agreeing points is 3, and they are not adjacent. The two swept parameters trade off rather than plateau: below about 2.5 x spacing the filter destroys real roof (838,168 points removed at 1.5 x spacing with fraction 1.0, against 3,134 at the loosest setting, a factor of 267). **The effect it removes is small where it is safe:** at the canonical cell the largest pitch change is 0.0141 deg and facet coverage moves 0.03 points. `canonical-2026-07-26-r2` is untouched; the filter is in the codebase OPT IN, defaulting off.
- **SCORECARD, P1-P9:** P1 UNSCOREABLE, P2 HELD, P3 FAILED, P4 FAILED, P5 UNSCOREABLE, P6 HELD, P7 FAILED, P8 HELD, P9 HELD. Qualifications that matter: **P4 failed because the quality bar ROSE at the destructive setting** (a fit metric got worse because a fix deleted real surface); **P7 failed on reporting resolution, not substance** (no facet lost zero points at any grid point, but at loose settings the pitch change rounded away below 0.00005 deg); **P8 held by luck** (its 79,000 bound was derived from post-trim points while the filter runs pre-trim, and the filter removes more than 79,000 at 10 of 20 grid points); **P6 cost nothing to make** (it deliberately claims no direction).
- **The two-channel result, and it refutes a stated expectation.** Emmett required the membership-to-coverage coupling be reported as two channels and expected the centroid channel might dominate for far one-sided strays. **It does not; it is negligible everywhere.** Centroids move up to 21.08 inches (facet 7) but almost entirely IN PLANE; the along-normal component never exceeds 0.0057 inches. The rotation channel is 1-2 orders larger. Pooling them would have overstated facet 7's plane movement by a factor of about 14,000.
- **Pre-registered count-versus-area guard: NEVER FIRED.** Largest-by-count and largest-by-area agreed at every facet at every grid point, so the "main body" definition was immaterial, exactly as the pre-registration said to record if so.

- **NEW STANDING RULE R5 (2026-07-28): raster PHASE is a parameter.** Any filter that rasterises coordinates and then reasons about adjacency or connectivity must sweep the origin phase, be invariant by construction, or fix the origin to something meaningful. Measured: a sub-cell phase shift swings facet 3's kept fraction from 0.5186 to 0.9978, **half the facet, 407,429 points**, at the same connectivity scale the sweep used. For scale, the two parameters that WERE swept moved coverage by 0.03 points. `decisions/2026-07-28-raster-phase-is-an-unswept-parameter.md`. **Open question, not an accusation:** `coverage_masks`, `residual_blobs` and the plan-cell area accounting all bin on a data-derived origin too, and none has been tested for phase sensitivity.
- **Standing rules (permanent):** R1 every run persists per-facet plane coefficients and inlier indices. R2 actionable diagnostics go to a file, never stdout only. R3 (2026-07-27) any depth-from-boundary or erosion measurement FILLS HOLES FIRST. R4 (2026-07-27) every diagnostic carries an assertion built from something known INDEPENDENTLY of its own result; register at `reports/silent-failures/README.md`. **R4 extension (2026-07-28): a probe that perturbs a parameter must assert that at least one perturbation changed at least one output.** A phase probe that shifted the points instead of the origin perturbed nothing and reported a spread of exactly 0.000000 with three passing checks; a null result looked stronger than a real one.

- **Canonical facet state, UNCHANGED:** `reports/big_house/canonical-2026-07-26-r2.json` + `.npz`, 29 facets (8 main + 21 recovered). Nothing in the 2026-07-27 or 2026-07-28 work regenerated it. **Verified this session:** sweep assertion A0 confirms the filter-off path reproduces it exactly, point counts bit-for-bit and pitch to 1e-3 deg. Published facet coverage **88.40 pct** stands and is a DEVELOPMENT READOUT on big_house, not a score.
- **TRIAGE RESULT (2026-07-27), seven mechanisms. 42 of 56 defect instances (75 pct) fit NEITHER pre-registered hypothesis:**

      M1  membership by distance to an UNBOUNDED plane      stage 1   11 facets   <- M1a CLOSED 2026-07-28
          M1a thin remote slivers                                     8 (all main)
          M1b bulk absorption of adjacent structure                   3 (8, 9, 10)
      M4  boundary stops inside the real edge               stage 4   17 facets
      M5  boundary ragged where the real edge is straight   stage 4   12 facets
      M3  one real plane fitted as TWO facets               stage 2   10 facets
      M2  dormer surfaces never discovered                  stage 2    3 surfaces
      M6  outline crosses a correctly-found ridge           stage 4    2 facets
      M7  neighbours disagree on a shared boundary          stage 4    1 facet

  M4 is the largest and was not pre-registered. Five defects are recorded UNATTRIBUTED and deliberately not forced into a bucket. Fix order is by pipeline STAGE, not severity.
- **CANDIDATE, logged 2026-07-27, NOT ADOPTED and not acted on:** boundary regularization, facet boundaries as straight lines with a shared boundary being ONE line. Eliminates M6, M7 and M5 by construction and part of M4. Blocked behind M1a, M2 and M3 because shared edges come from plane-plane intersection, which needs clean normals, present neighbours and non-coplanar pairs. Recorded as a MODEL CHANGE, not a bug fix. Stated cost: regularization can HIDE CAPTURE FAILURES and would break the detector that found blob 0; mitigation is to keep measured extent and regularized model as separate permanent outputs plus their divergence. `decisions/2026-07-27-candidate-boundary-regularization.md`.
- **Blob 0, CLARIFIED 2026-07-28 (no reversal).** Under the PRODUCTION config blob 0 has **no candidate at all**: its dominant plane, 162,938 points at 3.774 deg, is discarded by the recovery PITCH WINDOW (min_pitch 5.0) before quality is tested. The earlier finding that the QUALITY BAR is what holds it back came from a probe that opened that window to 1.0 so the plane could reach the quality test, where it failed by 0.0002 (2.9482005 against bar 2.9479968). Both are true of their own configuration. Consequence: **P5 was written asking a question the production pipeline never evaluates.** Indicative only: at the single grid point where the bar ROSE (2.9662), blob 0's historical candidate would clear it, which is the detector losing calibration rather than working.
- **M1a is measured, not just observed** (`reports/big_house/fragments-2026-07-27.json`): all 8 main facets carry disconnected fragments, extent inflation 1.21-2.40, strays up to 53.4 ft out, facet 0 resolving into 1,076 components. Independently reproduced 2026-07-28 by `probe_component_sizes.py` through a separate code path.
- **Recovered facet identity is STABLE (2026-07-28).** At the canonical cell all 21 recovered facets sit within 3.66 inches of a canonical recovered centroid, 18 of 21 within 0.26 inches, largest matched pitch move 0.11 deg. This answers the indirect half of P2, which the sweep itself could not (it persisted counts only).
- **Review loop, from pass 2 onward:** every pass produces a DIFF against the previous artifact, **behind a toggle, default OFF**. The review UI drops multiple choice for FREE TEXT ONLY. Mechanism attribution runs through Claude reading Emmett's prose: propose it and show it for confirmation, never infer a verdict silently, never fill a blank because the note seems clear.
- **OPEN DEFECT, must be settled before the bungalow freeze:** `scripts/canonical_state.py:184` calls `discover_facets` without `min_pitch`, so main discovery takes the default 10.0 while the file it writes records 5.0. Verified INERT on big_house. Not changed: it alters the production config and that is Emmett's call.
- **HELD, not cancelled:** Diagnostic E (alpha-shape area characterisation, alpha-radius plateau sweep, large-void flag) and the candidate fringe-attribution rule. Area pre-registration is deferred until E returns.
- **Area status:** the tape gave a LENGTH for scale; **no measured area exists for big_house**, so area has never been validated against ground truth. The scored total 3,559.3 ft^2 (2026-07-18) is 8 main facets only and excludes all 21 recovered facets and blob 0. No area total has ever been computed from the 29-facet state.
- **Unassigned footprint, split into its two causes (2026-07-27):** segmentation gap **33.682 cu^2 = 382.8 ft^2**; capture gap **49.902 cu^2 = 567.1 ft^2**, of which filled holes 44.974.
- **Last thing verified working (2026-07-28):** the full 5x4 sweep with six passing assertions (A0-A5); `verify_decisions_split.py` passes (53 entries, 29 pre-split byte-for-byte identical and in order, 24 appended); the same grid point run twice in separate processes gave bit-identical results, an unplanned determinism check.
- **Scored (2026-07-18), unaffected:** validation chain complete, both pre-registrations reported, failure first. Run 1 FAILED its scale cross-check at -3.96 pct; Run 2 PASSES at +1.92 pct. Quotable as evidence about cloud-unit geometry, scale and reproducibility, but NOT as an accuracy claim.
- **Results (run 2 scaling, 40.454 in/cu):** total roof area 3,559 ft^2 with a total-level dormer caveat. Pitch: max |error| 2.19 deg, PASS at 3 deg, not at 2 deg. **P7's consequence CHECKED, not assumed (2026-07-28):** M1a removal moves pitch by at most 0.0141 deg at the canonical cell, so the frozen pitch numbers were NOT meaningfully computed on contaminated membership.
