## Current state

- **Project goal (2026-07-21):** engineering PORTFOLIO project. Commercialization, customer acquisition, and pricing are dropped after a market analysis found no viable commercial path for a part-time solo operator (see the 2026-07-21 entry). Technical scope and the validation standard are unchanged.
- **SITE ROLES, decided 2026-07-27, binds everything below:** big_house is the **DEVELOPMENT** site. Visual inspection and iteration on it are unlimited, and **no accuracy claim ever comes from it**. bungalow and cove_house are **HELD-OUT VALIDATION** sites: rules frozen before either runs, each scored ONCE. **roger is lost**, so there are exactly TWO held-out sites and ONE attempt at each. See `decisions/2026-07-27-development-vs-validation-split.md`. The standing limit: visual review can establish THAT something is wrong and WHAT it is physically, and can NEVER set a parameter value.
- **Phase (2026-07-27):** big_house is in VISUAL REVIEW, the first pass. Renders are built and the verdict form is waiting on Emmett. The coverage/dormer track is paused behind that review, deliberately: three mechanisms for blob 0 have been proposed and killed by reasoning through summary statistics, and the whole roof has not yet been looked at once.
- **Active blocker:** none technical. Waiting on Emmett's first visual review pass.
- **Canonical facet state (2026-07-26), UNCHANGED:** `reports/big_house/canonical-2026-07-26-r2.json` + `.npz`, 29 facets (8 main + 21 recovered), `probability=1.0`, `min_pitch=5`, cell-based blob selection, derived point floor. Two guards pass every run: `points[idx]` rebuilds every facet bit for bit, and no point is owned by two facets. Nothing in the 2026-07-27 work regenerated it.
- **Headline coverage (2026-07-26), now a DEVELOPMENT READOUT on this site, not a score:** density-testable fraction (CAPTURE) **82.29 pct**; facet coverage (SEGMENTATION) **88.40 pct**, 256.574 of 290.256 cu^2. The 88.40 pct STANDS as the frozen historical number for that state and is not restated. On big_house it can now only go up under visual-review-driven changes, so it stops being a score here; its value as a detector transfers to bungalow and cove_house, computed once each after the rule freeze.
- **Blob 0 (2026-07-27): CONFIRMED REAL ROOF by physical inspection.** A lower, tree-occluded roof section of different material on the east elevation of the south wing; 11.5703 cu^2 plan (131.5 ft^2), candidate plane 16.5962 cu^2 gross over 162,938 points. The completeness invariant named it on 2026-07-26, before anyone looked, so this is a definition being applied and not ground truth tuning a parameter. NOTE the invariant was adopted 2026-07-21, AFTER the 2026-07-18 freeze, not before it.
- **Blob 0 mechanism status: THREE PROPOSED, THREE DEAD.** (1) two planes forced into one fit, REFUTED (0.267 deg apart, 0.425 in separation, interleaved not regional). (2) boundary contamination as the cause, WITHDRAWN (the depth transform ran on an unfilled mask, so "interior" selected DENSE regions, not DEEP ones; the same defect logged 2026-07-26 for the coverage denominator, reintroduced in a probe). (3) symmetric erosion as the fix, REFUTED (bar falls 0.239, blob 0 moves 0.001, never passes at any width; the `both_moved` pass condition did its job). **Blob 0 is real roof that fails the quality bar, and there is currently no defensible reason to think the bar is wrong.** One candidate remains and is deliberately NOT being probed until after the visual review: fit quality is a residual distance with no normalisation for capture density, and the bar is `max()` over 8 well-captured facets.
- **STANDING RULE added 2026-07-27 (a):** any depth-from-boundary or erosion measurement FILLS ENCLOSED HOLES FIRST, matching the footprint denominator rule. No exceptions, and it applies to diagnostics exactly as to production code.
- **STANDING RULE added 2026-07-27 (b):** every diagnostic carries at least ONE assertion that would fail loudly if the probe were measuring the wrong thing, built from something known INDEPENDENTLY of the probe's own result, and written into the output file with its pass/fail state. Four silent failures in one day motivated it (contaminated re-peel, erosion on an unfilled mask, a range taken across two coordinate axes, and an exit code read from `tail` rather than from python). None raised an error; all produced well-formed plausible output. See `decisions/2026-07-27-silent-failure-standing-rule.md`.
- **Magenta reconciliation (2026-07-27), resolved:** the render's single unassigned layer (83.584 cu^2 = 949.9 ft^2) and coverage's 33.682 cu^2 differed because of the BASE, not an error. Split and measured: **segmentation gap** (interior AND testable AND unexplained) = **33.682 cu^2 = 382.8 ft^2**, exactly coverage's number; **capture gap** (untestable or outside the eroded interior) = **49.902 cu^2 = 567.1 ft^2**, of which filled holes 44.974 and perimeter ring 4.928. The two are now drawn as separate layers because they call for opposite responses: a facet ringed by pink needs a segmentation fix, one ringed by blue needs a reflight.
- **Standing rules (permanent):** R1 every run persists per-facet plane coefficients and inlier indices to `reports/<dataset>/`. R2 any diagnostic Emmett is expected to act on is written to a file under `reports/<dataset>/`, never stdout only.
- **OPEN DEFECT, must be settled before the bungalow freeze:** `scripts/canonical_state.py:184` calls `discover_facets` without `min_pitch`, so main discovery takes `recon_common`'s default of **10.0**, while the file it writes records `min_pitch_deg=5.0` at line 299. Verified INERT on big_house (both values reproduce all 8 main facets exactly, so no published number moves), but it will not stay inert on a cloud with a genuine sub-10-degree main surface. Not changed: it alters the production config and that is Emmett's call.
- **HELD, not cancelled:** Diagnostic E (alpha-shape area characterisation, the alpha-radius plateau sweep, the large-void flag) and the candidate fringe-attribution rule. Both wait until after the first review pass. The coverage consequence of the fringe rule is pre-stated: it would raise facet coverage MECHANICALLY, since the cells it claims are exactly the cells currently counted as testable-but-unexplained, so a rise is not evidence it is right.
- **Area status:** the tape gave a LENGTH for scale; **no measured area exists for big_house**, so area has never been validated against ground truth and nothing is contaminated by the inspection. The scored total 3,559.3 ft^2 (2026-07-18) is 8 main facets only and excludes all 21 recovered facets and blob 0. **No area total has ever been computed from the 29-facet state.** Area pre-registration is deferred until Diagnostic E returns.
- **Visual review instrument (2026-07-27, new):** `scripts/review_render.py` draws the canonical state on the point cloud's OWN colour, rasterised top-down in the leveled frame. ODM produced no orthophoto for this dataset (the pipeline stops at `odm_georeferencing`); the cloud's colour was chosen over re-running ODM specifically to avoid a registration step, since a misaligned background would make correct facets look wrong. Outputs: `reports/big_house/review/2026-07-27/overview.png` plus 29 close-ups, and the verdict form `reviews/big_house/review-2026-07-27.json`.
- **Last thing verified working (2026-07-27):** 58 tests green as of 2026-07-26; `verify_decisions_split.py` passes. All 2026-07-27 probes carry embedded cross-checks that PASS: main facets reproduce `canonical-2026-07-26-r2` as index sets, blob 0's candidate reproduces `blob0-rejection-2026-07-26.json`, and the recomputed quality bar matches the canonical bar bit for bit.
- **Scored (2026-07-18), unaffected by any of the above:** validation chain complete, both pre-registrations reported, failure first. Run 1 (`6c2e614`) FAILED its scale cross-check at -3.96%; Run 2 (`4fe6859`, contiguity rule) PASSES at +1.92%. Ground truth committed (`4dd4f92`). Under the new site roles this remains quotable as evidence about cloud-unit geometry, scale and reproducibility, but NOT as an accuracy claim.
- **Results (run 2 scaling, 40.454 in/cu):** total roof area 3,559 ft^2 with a total-level dormer caveat (6 of 8 facets suspect; bias unquantified). Pitch: max |error| 2.19 deg, PASS at 3 deg, not at 2 deg. GPS scale error +2.75%.
- **Review instrument, schema v2 (2026-07-27):** IDENTITY and BOUNDARY are separate axes, because the overview pass showed most facets identified correctly while their outlines are sometimes in the wrong place; one `verdict` field recorded that as "correct" and lost the boundary error. Per facet: identity (correct/merge/split/spurious/unsure), boundary (tight/short/over/ragged/cut/unsure), severity (minor/moderate/major by area impact), location (compass), note. Plus top-level `missing_facets` and per-line verdicts for the 18 intersection lines (L0-L17, labelled on the overview). Filled in through `reports/big_house/review/2026-07-27/review.html`, a self-contained page opened from disk.
- **Emmett's overview observation (2026-07-27), recorded before the per-facet pass:** most facets are IDENTIFIED correctly; the errors are in LINE PLACEMENT and they are NOT systematic. This rules out a class of fixes in advance: no single threshold, offset or erosion width can correct an error that is not consistent, so the problem sits at the segmentation boundary rather than in a global parameter.
- **Next action:** Emmett completes the visual review in `review.html` and saves the exported JSON to `reviews/big_house/review-2026-07-27.json`. Then, in order: act on what the review names, run Diagnostic E, settle the `min_pitch` record/code defect, freeze the rules, and only then run bungalow.
